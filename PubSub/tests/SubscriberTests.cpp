#include <gtest/gtest.h>

namespace {

TEST(SubscriberTests, ConstructorRegistersCallbacksSkeleton)
{
    // TODO: Add fake publisher and assert both callbacks are registered.
    SUCCEED();
}

TEST(SubscriberTests, UserThenOrderFlowSkeleton)
{
    // TODO: Add test seam and assert user-to-order mapping behavior.
    SUCCEED();
}

} // namespace
